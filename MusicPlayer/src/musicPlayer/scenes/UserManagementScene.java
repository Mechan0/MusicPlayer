package musicPlayer.scenes;

import javafx.scene.Scene;

/**
 * @author Alexander Yaroslavtsev
 */
public class UserManagementScene {
	public static Scene getScene() {
		// this scene must list users
		// admin users must be able to make selected users on the list admins
		// and potentially revoke admin status too
		// it should be clear which users are admins
		throw new IllegalStateException("not implemented"); // TODO daniel
	}
}
